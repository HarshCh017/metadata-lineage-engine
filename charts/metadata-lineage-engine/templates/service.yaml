apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-metadata-lineage-engine
  labels:
    app: metadata-lineage-engine
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
  selector:
    app: metadata-lineage-engine
